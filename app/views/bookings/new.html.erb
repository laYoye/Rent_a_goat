<h1>Réserver ce troupeau :</h1>

<div class="col-4">
  <%= simple_form_for [@herd, @booking] do |f| %>
    <%= f.input :booking_date %>
    <%= f.submit "Submit", class: "btn btn-primary" %>
  <% end %>
</div>
